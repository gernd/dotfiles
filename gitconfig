[user]
    email = gernot.pointner@googlemail.com
    name = Gernot Pointner

[color]
ui = auto #for colored console output

[push]
    default = matching

[credential]
    helper = cache --timeout=3600

[alias]
# show outgoing commit, that have not been pushed
out = log --stat origin/master..HEAD
# simple colored log output
simplelog = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
